<%= form_for(@situacaodemanda) do |f| %>
  <% if @situacaodemanda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@situacaodemanda.errors.count, "error") %> prohibited this situacaodemanda from being saved:</h2>

      <ul>
      <% @situacaodemanda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
