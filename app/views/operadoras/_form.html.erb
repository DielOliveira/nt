<%= form_for(@operadora) do |f| %>
  <% if @operadora.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@operadora.errors.count, "error") %> prohibited this operadora from being saved:</h2>

      <ul>
      <% @operadora.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nomeo_da_operadora %><br>
    <%= f.text_field :nomeoperadora %>
  </div>

 </br>
 
  <button type="submit" class="btn btn-primary btn-sm">
    <span class="glyphicon glyphicon-floppy-disk"></span> Salvar
  </button>
  
<% end %>
