<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>NewTIME | Log in</title>
  <!-- Tell the browser to be responsive to screen width -->
  <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
  <!-- Bootstrap 3.3.6 -->
  <link href="/assets/template/bootstrap/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->

  <link rel="stylesheet" href="/assets/template/font-awesome/font-awesome.min.css" rel="stylesheet">
  <!-- Ionicons -->
  <link rel="stylesheet" href="/assets/template/ionicons/ionicons.min.css" rel="stylesheet">
  <!-- Theme style -->
  <link rel="stylesheet" href="/assets/template/dist/css/AdminLTE.min.css" rel="stylesheet">
  <!-- iCheck -->
  <link rel="stylesheet" href="/assets/template/plugins/iCheck/square/blue.css">


</head>
<br>
<body class="hold-transition login-page">
<div class="row">
  <!-- left column -->
  <div class="col-md-2">
  </div>
  <div class="col-md-8">
    <!-- general form elements -->
    <div class="box box-primary">
      <div class="box-header with-border">
        <h3 class="box-title">Enviar Mensagem</h3>
      </div>


<%= form_for(@contato) do |f| %>
  <% if @contato.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contato.errors.count, "error") %> prohibited this contato from being saved:</h2>

      <ul>
      <% @contato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="box-body">
  <div class="row mar-bot40">
    <div class="col-lg-12">
      <%= f.label :nome %><br>
      <%= f.text_field :nome, :class => 'form-control' %>
    </div>
  </div>
  <br>
  <div class="row mar-bot40">
    <div class="col-lg-12">
      <%= f.label :email %><br>
      <%= f.text_field :email, :class => 'form-control' %>
    </div>
  </div>
  <br>
  <div class="row mar-bot40">
    <div class="col-lg-12">
      <%= f.label :assunto_id %><br>
      <%= f.select :assunto_id, options_from_collection_for_select(Assunto.all, 'id', 'assunto',@contato.assunto_id), {}, {:class => 'form-control' } %>
    </div>
  </div>
  <br>
  <div class="row mar-bot40">
    <div class="col-lg-12">
      <%= f.label :mensagem %><br>
      <textarea name="contato[mensagem]" id="contato_mensagem" class="form-control" value="<%=@contato.mensagem%>"><%=@contato.mensagem%></textarea>      
    </div>
</div>

  <br>
  <div align="right">
    <%= link_to root_path, :class => "btn btn-info btn-bg" do %>
      <span class="glyphicon glyphicon-arrow-left"></span> Voltar
    <% end %>   
    <button type="submit" class="btn btn-success btn-bg">
      Enviar Mensagem
    </button>
  </div>
<% end %>

    </div>
    <!-- /.box -->
  </div>
</div></div>
</body>